---
globs: *.ts,*.tsx
alwaysApply: false
---
# TypeScript Standards

Type safety and TypeScript best practices for `.ts` and `.tsx` files.

## Type Safety

- **Strict TypeScript**: Prefer strict TypeScript with proper type definitions. Use type-safe patterns like Zod schemas for validation and type-safe DOM helpers where applicable.
